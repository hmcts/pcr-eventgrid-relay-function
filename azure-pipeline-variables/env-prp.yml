variables:
# No relay deployed to any live environment yet (DEPLOYMENT.md §9). Different Azure
# subscription/tenant entirely from dev/sit/ste — placeholder until that access exists.
- name: pcrServiceIngestionEndpoint
  value: "https://REPLACE_WITH_REAL_PRP_INGRESS_HOST/pcr/internal/hearing-results"
